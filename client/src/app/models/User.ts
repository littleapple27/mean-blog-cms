export class User {
  firstName: string;
  lastName: string;
  email: string;
  pw: string;
  pwConfirm: string;
  isAdmin?: boolean;
  //isLimited?: boolean;
  hide?: boolean;
  isActive?: boolean;
  icon?: any;
}
